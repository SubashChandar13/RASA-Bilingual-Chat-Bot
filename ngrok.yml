version: "2"
tunnels:
  rasa:
    addr: 5005
    proto: http
    subdomain: yoursubdomain  # Use your desired Ngrok subdomain (optional for free tier)
  flask:
    addr: 5000
    proto: http
    subdomain: yoursubdomain  # Same subdomain to expose both
